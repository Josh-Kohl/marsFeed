import React from 'react';

const Image = ({ image }) => {

  //render image, title, description, mars date, earth date
  //like button adds to local storage

  return (
    <div className='imageComponent'>
      <h1>Image Component</h1>
    </div>
  );
};

export default Image;